# ForgeStack
ForgeStack is a modular developer platform that generates complete application stacks using composable plugins.  Instead of manually wiring infrastructure, frameworks, and services, ForgeStack lets developers build production-ready systems from reusable components.
