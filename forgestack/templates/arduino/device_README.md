# Device Bridge

This folder is reserved for local device bridge assets generated by ForgeStack.

The initial M1.5-G scaffold is intentionally lightweight and is designed to support
first device bridge patterns without introducing a separate tool or repo.

Suggested future contents:

- local serial bridge helpers
- device-specific configs
- protocol adapters
- technician-side device utilities