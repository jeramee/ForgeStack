# Local Configuration

This folder is reserved for local-only workflow overrides.

Examples of future use:

- local paths
- environment overrides
- developer-specific settings
- local execution tuning

Do not commit sensitive local secrets to version control.