{
  "name": "{{ project_name|lower }}-frontend",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build"
  },
  "dependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0"{% if has_feature.charts %},
    "recharts": "^2.12.0"{% endif %}{% if has_feature.filters %},
    "@tanstack/react-query": "^5.59.0"{% endif %}
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}