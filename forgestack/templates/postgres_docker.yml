services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: {{ values.postgres.user }}
      POSTGRES_PASSWORD: {{ values.postgres.password }}
      POSTGRES_DB: {{ values.postgres.db }}
    ports:
      - "{{ values.postgres.port }}:5432"