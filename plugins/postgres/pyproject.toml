
[project]
name = "devscaffold-postgres"
version = "0.1.0"
dependencies = ["devscaffold"]

[project.entry-points."devscaffold.plugins"]
postgres = "devscaffold_postgres.plugin:plugin"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
